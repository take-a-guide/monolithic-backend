spring:
  datasource:
    url: jdbc:sqlserver://arquitetura-cloud.database.windows.net:1433;database=take-a-guide-db;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
    username: matrix.dba@arquitetura-cloud
    password: "Jefvacilao!"  # Substitua com sua senha do Azure SQL
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver  # Define o driver para o SQL Server
  application:
    name: take-a-guide
springdoc:
  swagger-ui:
    path: "/api-docs"
server:
  port: 8081
